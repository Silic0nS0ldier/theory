// inputs:
// - communication broker
export function start() {
    // loading up new tests (tests from different generations will be in different folders, so workers can be reused)
    // responding to events
}
