// can be converted into path string directly
type FullPath = unknown;

// Could be a path that makes sense given context (relative path) or a path part
// conversion to a path string should make nature of type abundantly clear
type PathFragment = unknown;
